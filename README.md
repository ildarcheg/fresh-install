# fresh-install

```
sudo git clone https://github.com/ildarcheg/fresh-install.git /fresh-install
sudo bash /fresh-install/scripts/download.sh
sudo bash /fresh-install/scripts/env.sh
sudo bash /fresh-install/scripts/fresh.sh -a sm -p acs
```
