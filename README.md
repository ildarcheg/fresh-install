# fresh-install

```
sudo git clone https://github.com/ildarcheg/fresh-install.git /fresh-install
sudo bash /fresh-install/download.sh
sudo bash /fresh-install/env.sh
sudo bash /fresh-install/fresh.sh -a sm -p acs
```
